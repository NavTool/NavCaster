syntax = "proto3";
package caster.core;

message StreamState {

  string uid = 1; // 连接唯一标识 (ConnectKey)

  uint64 online_time = 2; // 上线时刻
  uint64 update_time = 3; // 信息更新时刻

  uint64 send_total = 4;
  double send_speed = 5;
  uint64 recv_total = 6;
  double recv_speed = 7;

}