// 挂载点别名规则信息
syntax = "proto3";
package caster.context;

message AliasInfo {
  string uid = 1;

  uint64 create_time = 2; // 创建日期
  uint64 update_time = 3; // 修改日期

  string alias_name  = 4; // 挂载点的别名
  string source_name = 5; // 使用的源挂载点（当源挂载点上线的时候，别名挂载点也会同步上线）

}