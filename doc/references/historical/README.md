# Historical References

本目录保存早期需求、旧 Redis 说明和旧 Web 迁移资料。它们只作历史参考，不能作为当前实现事实。

| 文件 | 内容 |
| --- | --- |
| `requirements-legacy.md` | 早期功能要求。 |
| `redis-legacy.txt` | 旧 Redis key/场景说明。当前 schema 以 `current/redis-schema.md` 和源码为准。 |
| `frontend/casterweb-progress.md` | 旧 Web 迁移记录。当前 Web 位于 `web/`，不是旧 `app/CasterWeb/`。 |
