# References 文档说明

本目录保存外部协议、历史需求、旧资料和 proto 生成备忘。

这些资料有参考价值，但默认不是当前实现事实。进入开发任务前必须和源码、`current/` 文档、近期任务记录交叉核对。

| 目录 | 内容 |
| --- | --- |
| `protocols/` | NTRIP、PROXY 等协议资料。 |
| `historical/` | 早期需求、旧 Redis 说明、旧 Web 迁移记录。 |
| `proto/` | 旧 proto 生成命令备忘。 |
