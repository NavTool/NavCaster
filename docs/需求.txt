我希望能实现这么一个流程，首先Service可能有多个节点，每个节点会有一个唯一的ID，我希望能够在Redis中按照节点去存储配置文件，然后也可以直接修改每一个节点的配置来实时生效；因此我觉得程序至少要具备如下功能：生成或者配置节点唯一ID，且能够保证不重复（我预想的流程是这样的：启动程序 附带参数 -init 加节点名，如果没有设置节点名，那就随机生成一个节点名，并保存到本地，然后连接到Redis，将自己的配置写到Redis的节点字段中，之后启动程序的话就只需要配置连接到redis，然后配置从云端拉取，当然也可以设置不从云端拉取，使用本地模式）；redis配置修改后，本地能够获取到配置修改，应用更改；